# Streamlit secrets — gitignored. Restart Streamlit after editing.
#
# To finish setting this up:
#   1. Visit https://myaccount.google.com/security and enable 2-Step Verification.
#   2. Open https://myaccount.google.com/apppasswords
#   3. Pick "Mail" + "Other (Custom name)" → name it "PWN viewer"
#   4. Google shows a 16-character password — paste it below replacing
#      REPLACE_WITH_16_CHAR_APP_PASSWORD. Spaces are okay; Gmail strips them.
#
# Note: a regular Gmail account password will NOT work here. SMTP requires
# the dedicated App Password.

[smtp]
host = "smtp.gmail.com"
port = 465  # SMTPS (implicit SSL) — verified working on this network
user = "terrence.poole@gmail.com"
password = "ckgg olih lfqu zkom"
from_name = "Terry's Event Schedule"
